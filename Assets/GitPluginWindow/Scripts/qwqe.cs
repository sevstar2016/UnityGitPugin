using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class qwqe : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Соси пипиську");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
